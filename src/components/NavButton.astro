---
interface Props {
    label: string;
    href: string;
    current?: boolean;
}

const { label, href, current = false } = Astro.props as Props;
---

<li>
    <a
        href={href}
        class="px-3 py-2 text-ct-white inline-block relative font-bold"
        aria-current={current ? "page" : undefined}
    >
        <span
            class="relative inline-block before:absolute before:-inset-1 before:block before:skew-y-1 hover:before:-skew-y-2 before:bg-ct-black"
            ><span class="relative">{label}</span></span
        >
    </a>
</li>
