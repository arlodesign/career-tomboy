---
interface Props {
    href: string;
    label: string;
    ariaLabel?: string;
    /** Defaults to "_blank" for external links */
    target?: string;
}

const { href, label, ariaLabel, target = "_blank" } = Astro.props as Props;

// Only apply rel for new-tab links to avoid unnecessary attributes
const rel = target === "_blank" ? "noopener noreferrer" : undefined;
---

<a
    href={href}
    target={target}
    rel={rel}
    class="inline-flex items-center gap-1 border border-white px-3 py-1 text-sm font-medium text-white transition-colors hover:bg-white hover:text-black focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 cursor-pointer"
    aria-label={ariaLabel}
>
    <slot name="icon" />
    <span>{label}</span>
</a>
