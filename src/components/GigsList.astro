---
import Gig from "./Gig.astro";
import { getUpcomingGigs } from "../lib/gigs";
import Bg from "./Bg.astro";

const upcomingGigs = getUpcomingGigs();
---

<section id="gigs" class="page-section">
    <Bg colorVar="--color-ct-strawberry" />
    <div class="container mx-auto max-w-3xl px-4 lg:px-5">
        <h2 class="mb-8 mt-0 text-center font-heading text-4xl italic">
            See us live!
        </h2>

        <div
            id="gigs-list"
            role="feed"
            aria-label="Upcoming shows"
            class="mt-4 flex flex-col gap-8"
        >
            {
                upcomingGigs.length > 0 ? (
                    <>
                        {upcomingGigs.map((gig) => (
                            <Gig gig={gig} />
                        ))}
                    </>
                ) : (
                    <p class="py-8 text-center text-lg text-slate-700">
                        No upcoming shows scheduled. Check back soon!
                    </p>
                )
            }
        </div>
    </div>
</section>
